; ##################################################################################################
; # 
; # データ部分（アニメーション）
; # 
; ##################################################################################################

; アニメーション定義
; 現在は1フレームで同時に2枚以上の画像を表示する機能なし
; そのため START ～ START+RANGE が複数のfexecでヒットしても最初にマッチしたものしか表示されない

	; 斬撃（剣）
	id = 1
	anime_info(id,ANIME_INFO_IMGDEF) = 1		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 命中エフェクト
	id = 2
	anime_info(id,ANIME_INFO_IMGDEF) = 2		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 2		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 0		; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 0		; 命中時のアニメーション
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = 5
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = 6
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = 7
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = 8
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 8	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 上矢印
	id = 3
	anime_info(id,ANIME_INFO_IMGDEF) = 3		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 3		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 14	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 0		; 命中時のアニメーション
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 8	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 9
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 9	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 下矢印
	id = 4
	anime_info(id,ANIME_INFO_IMGDEF) = 4		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 4		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 14	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 0		; 命中時のアニメーション
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 8	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 9
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 9	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	
	; 風（弱）
	id = 5
	anime_info(id,ANIME_INFO_IMGDEF) = 5		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 5		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 1		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 21	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 29	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	
	; 横斬撃
	id = 6
	anime_info(id,ANIME_INFO_IMGDEF) = 6		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 1	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	
	; 横薙ぎ払い
	id = 7
	anime_info(id,ANIME_INFO_IMGDEF) = 6		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 6		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 1	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 横薙ぎ払い（赤）
	id = 8
	anime_info(id,ANIME_INFO_IMGDEF) = 7		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 6		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 9		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 1	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 火（弱）
	id = 9
	anime_info(id,ANIME_INFO_IMGDEF) = 8		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 7		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 20	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 1		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 氷（弱）
	id = 10
	anime_info(id,ANIME_INFO_IMGDEF) = 9		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 8		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 20	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 雷（弱）
	id = 11
	anime_info(id,ANIME_INFO_IMGDEF) = 10		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 9		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 20	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 光（弱）
	id = 12
	anime_info(id,ANIME_INFO_IMGDEF) = 11		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 10		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 闇（弱）
	id = 13
	anime_info(id,ANIME_INFO_IMGDEF) = 12		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 11		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 回復（弱）
	id = 14
	anime_info(id,ANIME_INFO_IMGDEF) = 13		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 12		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 20	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 14	; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 魔法陣生成
	id = 15
	anime_info(id,ANIME_INFO_IMGDEF) = 14		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 13		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 0		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 21	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか


	; ふり下ろし
	id = 16
	anime_info(id,ANIME_INFO_IMGDEF) = 15		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 14		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 連打
	id = 17
	anime_info(id,ANIME_INFO_IMGDEF) = 16		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 15		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 地面に衝撃
	id = 18
	anime_info(id,ANIME_INFO_IMGDEF) = 17		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 16		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 7		; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 刺す（上から下）
	id = 19
	anime_info(id,ANIME_INFO_IMGDEF) = 18		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 17		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 13	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 叩きつける
	id = 20
	anime_info(id,ANIME_INFO_IMGDEF) = 19		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 18		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 7		; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 火（強）
	id = 21
	anime_info(id,ANIME_INFO_IMGDEF) = 20		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 19		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 20	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 氷（強）
	id = 22
	anime_info(id,ANIME_INFO_IMGDEF) = 21		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 20		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 20	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 風（強）
	id = 23
	anime_info(id,ANIME_INFO_IMGDEF) = 22		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 21		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 1		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 21	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 29	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 雷（強）
	id = 24
	anime_info(id,ANIME_INFO_IMGDEF) = 23		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 22		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 20	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 光（強）
	id = 25
	anime_info(id,ANIME_INFO_IMGDEF) = 24		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 23		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 9
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 闇（強）
	id = 26
	anime_info(id,ANIME_INFO_IMGDEF) = 25		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 24		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 9
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; ハート
	id = 27
	anime_info(id,ANIME_INFO_IMGDEF) = 26		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 25		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 21	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 21	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 29	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 光の収束
	id = 28
	anime_info(id,ANIME_INFO_IMGDEF) = 27		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 26		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 26	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 0		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 5	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -70	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -100	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 6	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 5	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -70	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -60	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 5	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -70	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -20	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 5	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -70	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 21	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 5	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -70	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 26	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 5	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -70	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 31	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 5	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -70	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 淡い光の発散
	id = 29
	anime_info(id,ANIME_INFO_IMGDEF) = 28		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 27		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 17	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 21	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 29	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 横薙ぎ払い（赤・強）
	id = 30
	anime_info(id,ANIME_INFO_IMGDEF) = 7		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 6		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 21	; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 1	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 闇（弱）と火（弱）
	id = 31
	anime_info(id,ANIME_INFO_IMGDEF) = 12		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 11		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 9		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; ふり下ろして地面に衝撃
	id = 32
	anime_info(id,ANIME_INFO_IMGDEF) = 15		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 14		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 18	; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 斬撃（剣）＆下矢印
	id = 33
	anime_info(id,ANIME_INFO_IMGDEF) = 1		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 4		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 横薙ぎ払い＆光（弱）
	id = 34
	anime_info(id,ANIME_INFO_IMGDEF) = 6		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 6		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 12	; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 1	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 闇（強）＆下矢印
	id = 35
	anime_info(id,ANIME_INFO_IMGDEF) = 25		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 24		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 4		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 9
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 横斬撃＆下矢印
	id = 36
	anime_info(id,ANIME_INFO_IMGDEF) = 6		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 4		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 1	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	
	; 横斬撃＆火（弱）
	id = 37
	anime_info(id,ANIME_INFO_IMGDEF) = 6		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 9		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 1	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 斬撃（剣）＆火（弱）
	id = 38
	anime_info(id,ANIME_INFO_IMGDEF) = 1		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 9		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	
	; 斬撃（剣）＆氷（弱）
	id = 39
	anime_info(id,ANIME_INFO_IMGDEF) = 1		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 10		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	
	; 斬撃（剣）＆雷（弱）
	id = 40
	anime_info(id,ANIME_INFO_IMGDEF) = 1		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 11		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	
	; 連打＆雷（弱）
	id = 41
	anime_info(id,ANIME_INFO_IMGDEF) = 16		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 15		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 11		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 横斬撃＆風（弱）
	id = 42
	anime_info(id,ANIME_INFO_IMGDEF) = 6		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 5		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 1	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; どくろ
	id = 43
	anime_info(id,ANIME_INFO_IMGDEF) = 29		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 33		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 17	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 21	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 29	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 紫のもや
	id = 44
	anime_info(id,ANIME_INFO_IMGDEF) = 30		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 34		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 17	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 21	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 29	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 超音波
	id = 45
	anime_info(id,ANIME_INFO_IMGDEF) = 31		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 35		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 17	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 21	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 29	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 4	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 斬撃（剣）＆紫のもや
	id = 46
	anime_info(id,ANIME_INFO_IMGDEF) = 1		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 44		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 叩きつける＆紫のもや
	id = 47
	anime_info(id,ANIME_INFO_IMGDEF) = 19		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 18		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 7		; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 44	; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; ふり下ろし＆紫のもや
	id = 48
	anime_info(id,ANIME_INFO_IMGDEF) = 15		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 14		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 44	; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; ふり下ろし＆雷（強）
	id = 49
	anime_info(id,ANIME_INFO_IMGDEF) = 15		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 14		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 24	; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 10	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = -40	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 闇（弱）＆どくろ
	id = 50
	anime_info(id,ANIME_INFO_IMGDEF) = 12		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 11		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 43		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 闇（強）＆どくろ
	id = 51
	anime_info(id,ANIME_INFO_IMGDEF) = 25		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 24		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 43		; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 9
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 光（弱）＆どくろ
	id = 52
	anime_info(id,ANIME_INFO_IMGDEF) = 11		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 10		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 25	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 43	; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 4	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 10	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 16	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 19	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 22	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 25	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 28	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 3	; 何フレーム同じ画像を表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか

	; 斬撃（剣）＆どくろ
	id = 53
	anime_info(id,ANIME_INFO_IMGDEF) = 1		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 43	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 2		; 命中時のアニメーション
	
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 1	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 2	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 3	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 4	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 5	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 6	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 7	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = 0	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; 横斬撃＆紫のもや
	id = 54
	anime_info(id,ANIME_INFO_IMGDEF) = 6		; 使用するアニメーション画像定義ID
	anime_info(id,ANIME_INFO_FRAME_SOUND) = 1	; 効果音を出すフレーム数
	anime_info(id,ANIME_INFO_SOUNDID) = 1		; 効果音ID
	anime_info(id,ANIME_INFO_FRAME_HIT) = 11	; 命中判定のあるフレーム数
	anime_info(id,ANIME_INFO_HIT_ANIME) = 44	; 命中時のアニメーション
	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
	fexec = 0
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 1	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 1	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 1
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 3	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 2
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 5	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 3
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 7	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 4
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 9	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 5
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 11	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 6
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 13	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 7
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 15	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	fexec = 8
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 17	; 何フレームから表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 2	; 何フレーム同じ画像を表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = -20	; 基準からX軸でどれだけずらして表示するか
	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	;fexec = x
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
	
;	; 説明
;	id = x
;	anime_info(id,ANIME_INFO_IMGDEF) = 0		; 使用するアニメーション画像定義ID
;	anime_info(id,ANIME_INFO_FRAME_SOUND) = 0	; 効果音を出すフレーム数
;	anime_info(id,ANIME_INFO_SOUNDID) = 0		; 効果音ID
;	anime_info(id,ANIME_INFO_FRAME_HIT) = 0		; 命中判定のあるフレーム数
;	anime_info(id,ANIME_INFO_HIT_ANIME) = 0		; 命中時のアニメーション
;	anime_info(id,ANIME_INFO_REVERSE) = 0		; 左右逆転(1:する)
;	fexec = 0
;	anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
;	anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
;	anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
;	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
;	anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか
;	;fexec = x
;	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_IMGID) = fexec	; 何枚目の画像を使うか
;	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_START) = 0	; 何フレームから表示するか
;	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_RANGE) = 0	; 何フレーム同じ画像を表示するか
;	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETX) = 0	; 基準からX軸でどれだけずらして表示するか
;	;anime_info_frame(id,fexec,ANIME_INFO_FRAME_OFFSETY) = 0	; 基準からY軸でどれだけずらして表示するか

	; ANIME_INFO_FRAME_FINISH（表示終了するフレーム数）を計算
	for i,0,ANIME_MAX
		if anime_info(i,ANIME_INFO_IMGDEF) > 0 {
			anime_info(i,ANIME_INFO_FRAME_FINISH) = 0
			for j,0,ANIME_EXEC_MAX
				tmp_max = anime_info_frame(i,j,ANIME_INFO_FRAME_START) + anime_info_frame(i,j,ANIME_INFO_FRAME_RANGE)
				if anime_info(i,ANIME_INFO_FRAME_FINISH) < tmp_max {
					anime_info(i,ANIME_INFO_FRAME_FINISH) = tmp_max
				}
			next
		}
		if DEBUG_MODE : logmes strf("D ANIME_INFO_FRAME_FINISH animeid=%d, finish=%d",i,anime_info(i,ANIME_INFO_FRAME_FINISH))
	next
	